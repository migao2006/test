{
  "name": "taiwan-stock-smart-picker",
  "version": "15.5.0",
  "displayName": "台股智慧選股",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "bash scripts/build.sh",
    "validate": "node scripts/validate-artifact.mjs",
    "smoke": "node scripts/smoke.mjs",
    "audit": "node scripts/audit-live.mjs",
    "test": "npm run build && npm run validate && npm run smoke"
  }
}
